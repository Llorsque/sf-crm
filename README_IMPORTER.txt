Upload deze twee bestanden:
- app.js                (voegt automatisch een 📥 Import link toe aan je menu)
- pages/import.js       (de CSV-import pagina)

Daarna: hard refresh (of DevTools -> Network -> Disable cache -> Reload).
Je ziet nu in het menu **📥 Import**. Open die, kies tabel + match kolom, upload CSV, map kolommen en run.
